/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://nexomark.agency', // replace with your live site URL
  generateRobotsTxt: true, // (optional)
  changefreq: 'weekly',
  priority: 0.7,
  sitemapSize: 7000,
};
